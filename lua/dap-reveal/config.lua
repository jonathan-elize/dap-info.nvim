local config = {}

config = {
  on_load_breakpoint = nil,
  focus_id = "dap-info",
  virt_text_opts = {
    namespace = "dap-info",
    prefix = "●",
    suffix = "",
    spacing = 4,
  },
}

return config
